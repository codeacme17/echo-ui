---
title: 声明
description: 了解 Echo UI 组件、Hook 和文档的设计边界。
---

# 声明

## 关于组件

**Echo UI 是音频界面组件库，而不是完整的应用 UI 套件。** 它不以提供通用表单、数据表格或列表为目标。即使是按钮和输入框等常见控件，其 API 也围绕音频交互设计。

当应用同时需要音频工具和常规产品 UI 时，可以将 Echo UI 与更全面的组件系统配合使用。[shadcn/ui](https://ui.shadcn.com/) 是可选方案之一，具体选择应以应用的框架和无障碍需求为准。

## 关于 Hook

Echo UI `1.1.x` 使用并测试 Tone.js `15.1.22`。Tone 保持为直接运行时依赖，因为公开 Hook 声明会暴露 `Tone.Player`、`Tone.Analyser` 与 `Tone.InputNode` 等类型。ESM 与 UMD 构建会将 Tone 外部化，而不会内嵌第二份副本，因此已安装的消费者会解析到同一个兼容的 Tone 15 运行时，并共享一致的 Web Audio 上下文与类标识。

应用通常无需单独安装 Tone。若应用会直接引入 Tone，请保持使用 `^15.1.22`，以便包管理器完成去重。通过 script 标签使用 UMD 构建时，需要像 React 一样同时提供 Tone 全局变量。事先阅读 [Tone.js 文档](https://tonejs.github.io/)，可以更容易理解 Hook 的时序与音频节点行为。纯展示组件仍可接入其他音频库，或直接使用原生 Web Audio API。

## 关于本文档

本站使用 [Nextra](https://nextra.site/) 构建，并从软件包所在的同一仓库导出静态站点。中英文指南共享相同的路由结构，切换语言时不会丢失当前阅读位置。

欢迎通过 [GitHub Issues](https://github.com/codeacme17/echo-ui/issues) 或每个页面上的编辑链接完善文档。
