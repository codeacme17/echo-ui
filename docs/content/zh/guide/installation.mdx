---
title: 安装
description: 安装当前的 Echo UI 发行版并配置已验证的使用要求。
---

import { Callout, Steps, Tabs } from 'nextra/components'

# 安装

<Callout type="info">
  当前 npm 发行版 **`@nafr/echo-ui@1.0.0`** 声明了 **React 18.2** 及对应 React DOM 的 peer dependency。下一个发行版会将范围扩展为 **React 18.2 或 React 19**，并要求 React DOM 使用相同的主版本。开发环境与文档站使用 React `19.2.8`；已安装消费者冒烟测试会使用 React `18.3.1` 和 `19.2.8` 渲染构建后的软件包。
</Callout>

## 开始之前

Echo UI `1.0.0` 使用 Tailwind CSS `3.3.5` 构建并编写示例，但它的已发布 manifest 没有声明 Tailwind peer dependency 或兼容范围。下方配置仅针对 Tailwind CSS 3；尚未验证与 Tailwind 后续大版本的兼容性。

<Steps>

### 安装软件包

<Tabs items={['pnpm', 'npm', 'Yarn']} storageKey="echo-ui-package-manager">
  <Tabs.Tab>

  ```bash
  pnpm add @nafr/echo-ui@1.0.0
  ```

  </Tabs.Tab>
  <Tabs.Tab>

  ```bash
  npm install @nafr/echo-ui@1.0.0
  ```

  </Tabs.Tab>
  <Tabs.Tab>

  ```bash
  yarn add @nafr/echo-ui@1.0.0
  ```

  </Tabs.Tab>
</Tabs>

### 配置 Tailwind CSS 3

将已发布的组件文件加入 Tailwind 的内容扫描范围，并在 `tailwind.config.js` 中映射 Echo UI 的语义 token：

```js filename="tailwind.config.js"
/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    './src/**/*.{js,ts,jsx,tsx}',
    './node_modules/@nafr/echo-ui/dist/**/*.{js,cjs,mjs}',
  ],
  darkMode: 'class',
  theme: {
    extend: {
      colors: {
        input: 'var(--echo-input)',
        background: 'var(--echo-background)',
        foreground: 'var(--echo-foreground)',
        border: {
          DEFAULT: 'var(--echo-border)',
          foreground: 'var(--echo-border-foreground)',
        },
        button: {
          DEFAULT: 'var(--echo-button)',
          foreground: 'var(--echo-button-foreground)',
        },
        primary: {
          DEFAULT: 'var(--echo-primary)',
          foreground: 'var(--echo-primary-foreground)',
        },
        secondary: {
          DEFAULT: 'var(--echo-secondary)',
          foreground: 'var(--echo-secondary-foreground)',
        },
        muted: {
          DEFAULT: 'var(--echo-muted)',
          foreground: 'var(--echo-muted-foreground)',
        },
        accent: {
          DEFAULT: 'var(--echo-accent)',
          foreground: 'var(--echo-accent-foreground)',
        },
        card: {
          DEFAULT: 'var(--echo-card)',
          foreground: 'var(--echo-card-foreground)',
        },
      },
      borderRadius: {
        lg: 'var(--echo-radius)',
        md: 'calc(var(--echo-radius) - 2px)',
        sm: 'calc(var(--echo-radius) - 4px)',
      },
    },
  },
}
```

### 添加主题变量

在主样式表中引入 Tailwind 层和 Echo UI token。下方的数值与已发布文档主题一致，也可以按项目需求调整。

```css filename="src/index.css"
@tailwind base;
@tailwind components;
@tailwind utilities;

:root {
  --echo-background: #f1efef;
  --echo-foreground: #181818;
  --echo-card: #dadada;
  --echo-card-foreground: #2a2a2a;
  --echo-primary: #ffaa01;
  --echo-primary-foreground: #e49c0c;
  --echo-secondary: #f3b945;
  --echo-secondary-foreground: #b7944e;
  --echo-muted: #c4c4c4;
  --echo-muted-foreground: #393939;
  --echo-accent: #f4f4f5;
  --echo-accent-foreground: #2e2e2e;
  --echo-border: #c8c8c8;
  --echo-border-foreground: #494949;
  --echo-button: #d3d3d3;
  --echo-button-foreground: #262626;
  --echo-input: #e4e4e7;
  --echo-wave: #9e9e9e;
  --echo-radius: 0.5rem;
}

.dark {
  --echo-background: #181818;
  --echo-foreground: #f1efef;
  --echo-card: #2f2f2f;
  --echo-card-foreground: #dadada;
  --echo-primary: #e49c0c;
  --echo-primary-foreground: #ffaa01;
  --echo-secondary: #b7944e;
  --echo-secondary-foreground: #f3b945;
  --echo-muted: #393939;
  --echo-muted-foreground: #c4c4c4;
  --echo-accent: #27272a;
  --echo-accent-foreground: #f4f4f5;
  --echo-border: #494949;
  --echo-border-foreground: #c8c8c8;
  --echo-button: #262626;
  --echo-button-foreground: #d3d3d3;
  --echo-input: #2a2a2a;
  --echo-wave: #7d7d7d;
}
```

### 引入组件

```tsx filename="src/App.tsx"
import { Knob } from '@nafr/echo-ui'

export default function App() {
  return <Knob />
}
```

</Steps>

可以在 [`packages/tailwind-theme.ts`](https://github.com/codeacme17/echo-ui/blob/main/packages/tailwind-theme.ts) 和 [`packages/index.css`](https://github.com/codeacme17/echo-ui/blob/main/packages/index.css) 查看持续维护的主题源码。
